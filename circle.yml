machine:
  services:
    - docker

dependencies:
  pre:
    - wget -O /home/ubuntu/bin/gh-release https://github.com/progrium/gh-release/releases/download/v1.0.0/gh-release
    - chmod +x /home/ubuntu/bin/gh-release
  override:
    - cd cedar14 && make build

deployment:
  release:
    branch: refactor
    commands:
      - make release
