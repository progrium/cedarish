machine:
  services:
    - docker

dependencies:
  pre:
    - make deps
  override:
    - make -C cedar14 build

test:
  override:
    - make -C cedar14 test

deployment:
  release:
    branch: release
    commands:
      - make release
